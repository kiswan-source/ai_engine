import { PhasePlaceholder } from '@/components/layout/PhasePlaceholder'

export default function ProjectsPage() {
  return (
    <PhasePlaceholder
      title="Projects"
      phase="Phase 3"
      reason="belum ada entity Project di backend (PROJECT_SPECIFICATION.md — spesifikasi awal, belum diimplementasikan)."
    />
  )
}
