# --- Secrets / environment ---
.env
.env.*
!.env.example

# --- Python ---
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/

# --- Runtime data / output ---
audit.log
*.log
# Rotation backups (security_audit.log.1, .2, ...) and the flock lock file
# (Fase 1 / SEC-8, security/audit_log.py) — *.log above doesn't match these
# since the .log suffix isn't at the end.
security_audit.log.*
# Flock lock file for the Continuous Improvement Engine's ledger (Fase 7,
# improvement/ledger.py) — same reasoning as security_audit.log.* above.
improvement_ledger.log.lock
backups/
uploads/
reports/

# --- OS / editor ---
.DS_Store
*.swp
.idea/
.vscode/
