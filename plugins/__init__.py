"""Plugins (MASTER_INSTRUCTION.md Bab 59) — optional capabilities registered
into the existing tool registry (`agent/tools/registry.py`), never a system
Orchestrator/Workflow reach into directly. See `plugins/base.py` for the
contract and `registry/plugin_registry.py` for the catalog.
"""
